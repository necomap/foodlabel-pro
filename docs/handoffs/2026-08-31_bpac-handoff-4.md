# FoodLabel Pro — 引き継ぎ文書④（Excelインポート改善・管理者分析・運用メモ）

作成日：2026年8月31日
対象：「2026-08-30_bpac-handoff-3.md」（引き継ぎ文書③）の続きを別チャットで引き継ぐ人
前提：この文書は「FoodLabel Pro — 引き継ぎ文書.md」（8/19作成）→「①」→「②」→「③」の続きです。プロジェクト概要・絶対厳守事項（`prisma migrate dev`禁止、`prisma db push`のみ使うこと等）はそちらを参照してください。
補足：このチャットでも`device_bash`（ユーザーPC上でコマンドを直接実行するツール）が使えず、ファイル編集は「ステージ→ローカル編集→ユーザーへ送付→コミット（mtime確認付き）」の手順で行った。

---

## 0. このチャットで対応したことの要約

1. 使い方ページ（`app/dashboard/help/page.tsx`）にb-PAC印刷の説明を追加。管理者アカウント関連の文言を全体から削除。
2. プレミアム／プロプランでExcelインポート・エクスポートに差をつけた（プレミアム：月5回まで、プロ：無制限）。`data_transfer_logs`テーブルを新設（**要確認事項、1章参照**）。
3. `next build`のTypeScriptエラーを2件修正（`lib/bpac-print.ts`の動的import、Stripe `Charge.invoice`型エラー）。
4. 返金時に自動でプランをダウングレードする処理（`charge.refunded`のwebhook）を追加。**この対応より前に返金されたアカウントは対象外**（手動でのダウングレードが必要だが、現状は本番の該当アカウント無しとユーザーに確認済み）。
5. 管理者ダッシュボードのユーザー分析を拡張（**このチャットで実際に実装・コミット済み。4章に重要な訂正あり**）。
6. b-PACラベルの「あたり」「製造者：」重複表示バグを修正（**5章**）。
7. b-PACキャプション表示切り替え機能を一度実装したが、ユーザーの意図と違ったため撤去（**5章**）。
8. Excelインポートの「1回最大80件」制限について、ユーザー操作としては1ボタンで大量データも自動処理できるように改善（**6章**）。
9. CSV対応は見送り（Excelのみのまま）で確定。
10. npm・Prismaのバージョン絡みの運用メモ（**2章、ユーザーから明記依頼あり**）。

---

## 1. 【要確認・最優先】`data_transfer_logs`のスキーマ、`prisma db push`は実行済みか？

このチャットより前の段階で、プラン別インポート・エクスポート回数制限のために`prisma/schema.prisma`へ`data_transfer_logs`モデルを追加し、ユーザーに「`npx prisma db push`をご自身で実行してください」と依頼した。

**このチャットでは実行有無を確認できていない。** 次のチャットで最初に確認すること。もし未実行の場合、以下の影響が出る：

- `lib/plan-limits.ts`の`getMonthlyDataTransferCount()`は`prisma.$queryRaw`で直接`data_transfer_logs`テーブルを見に行っており、**try/catchで囲まれていない**。テーブルが存在しないとインポート・エクスポートAPI（`/api/import-export`）がエラーで失敗する可能性がある。
- 一方`logDataTransfer()`（書き込み側）は`try/catch`で囲まれ、失敗してもconsole.warnのみで処理を止めない設計なので、こちらは実害が出にくい。

まず「実際にインポート・エクスポートを試して、月次回数チェックがエラーなく動くか」をユーザーに確認してもらうのが早い。

---

## 2. 【運用メモ・ユーザーから記載依頼あり】npm / Prisma バージョン関連

- ユーザーのnpmは11.9.0で、`npm approve-scripts`コマンドが存在しなかった（このコマンドはnpm 11.16.0で追加された機能）。
- `npm install -g npm@latest`で最新化しようとすると、npm 12.0.2は`EBADENGINE`エラーになる（`node ^22.22.2 || ^24.15.0 || >=26.0.0`が必要。ユーザーのNodeはv24.14.0で非対応）。
- 回避策として`npm install -g npm@11`（11系の最新＝11.19付近）を案内し、これで`npm approve-scripts --all`が実行できた。
- 結果、`package.json`に以下の`allowScripts`が追加された：
  ```json
  "allowScripts": {
    "@prisma/client@6.19.2": true,
    "@prisma/engines@6.19.2": true,
    "prisma@6.19.2": true,
    "unrs-resolver@1.11.1": true
  }
  ```
- **今後の注意点**：ここはバージョン番号まで固定して承認する形式になっている。今後Prismaをアップデートすると、新しいバージョンに対して同じ「N packages have install scripts not yet covered by allowScripts」という警告が再度出る可能性が高い。その場合はコード修正不要で、ユーザーに再度`npm approve-scripts --all`を実行してもらえばよい（本人へすでにその旨は伝達済みだが、忘れる可能性があるため再度こちらにも記載）。
- これはあくまで警告であり、現時点でビルドを壊すものではない（npmが将来のバージョンで未承認スクリプトを実際にブロックするようになった場合に初めて問題になる）。

---

## 3. 【未解決・仕様確認が必要】非表示にしたレシピを再インポートすると新規作成される

**ユーザーからの質問**：「インポート→未使用のレシピを非表示→再度インポート、とした場合、未使用レシピの分は新しく作成されてしまう？」

**コードを確認した結果、その通りになることを確認済み（未修正）**。

原因：`app/api/import-export/route.ts`の重複チェック・上書き処理が、どちらも`isActive: true`のレシピのみを対象にしている。

```ts
const existingRecipes = await prisma.recipe.findMany({
  where: { userId: session.user.id, isActive: true },   // ← 非表示(isActive:false)は含まれない
  select: { id: true, name: true },
});
```

- 上書きOFFの場合：同名チェック（`recipeCache.get(name)`）で非表示レシピはヒットしないため、スキップされず新規作成される。
- 上書きONの場合：既存を論理削除する`updateMany`も`isActive: true`だけを対象にしているため、非表示レシピは対象にならず素通りし、結局新規作成される。

結果として、非表示レシピはDBに残り続け（今の仕様では「非表示」と「論理削除」が同じ`isActive`フィールドを共有している）、同名の新しいレシピが別レコードとして積み重なっていく。

**このチャットでは未修正**（バグとも仕様とも取れるため、対応するかどうかユーザーへの確認が必要と判断し保留にした）。対応する場合の選択肢の例：

1. 重複チェックを`isActive`を問わず名前一致で見るようにし、非表示レシピも「上書き対象」（＝再インポートで復活）として扱う。
2. 非表示レシピを画面から一括削除できる機能を別途用意し、定期的に整理してもらう運用にする。
3. 現状維持（非表示＝そのユーザーにとって「もう要らないもの」という前提なら、新規作成される方が自然という考え方もある）。

次のチャットでユーザーに意向を確認してから対応すること。

---

## 4. 【今回実装・重要な訂正あり】管理者ダッシュボードのユーザー分析拡張

`app/api/admin/stats/route.ts`と`app/admin/page.tsx`に以下を追加した：

- 登録のみユーザー数（レシピ0件のアクティブユーザー）
- 解約済み（元有料）ユーザー数：現在フリープランだが、過去に`Subscription`で`status: active`または`past_due`（＝実際に課金された）履歴があるユーザー
- お試しのみユーザー数：`Subscription`履歴はあるが、一度も`active`/`past_due`に至らなかった（トライアル中にキャンセル等）ユーザー
- レシピ数のユーザーごとの最大・最小・平均（`Recipe.groupBy`で集計）
- 食材マスタ数のユーザーごとの最大・最小・平均（`Ingredient.groupBy`、`userId`がnullの共有マスタ等は除外）

「アクティブユーザー」（リアルタイムのログイン状況）は、ログイン履歴を記録するスキーマ変更が必要になるため対象外。ユーザー本人から「そこは今回のイメージと違う（登録後に使い始めたかどうかが知りたかっただけ）ので着手しなくてよい」と明示的に確認済み。上記の「登録のみユーザー」の裏返し（総ユーザー数－登録のみユーザー数）で実質的に代用できる想定。

**重要な訂正**：これより前のチャット（このチャットの会話要約）では、この機能は「実装・コミット済み」と記載されていたが、**このチャットの冒頭で実ファイルを確認したところ、実際にはコード上に一切存在しておらず未実装だった**（要約の誤り、または前チャットでの作業がユーザーPCへ実際には保存・コミットされないまま終わっていたと思われる）。「管理者ダッシュボードのレイアウトがデプロイ後も変わらない」というユーザーからの指摘で発覚した。`prisma db push`忘れが原因ではなく、単純に未実装だった。

**教訓**：次のチャットに限らず、過去チャットの要約に「実装・コミット済み」と書かれていても鵜呑みにせず、疑わしい場合は実際のファイルをステージして中身を確認してから作業を始めた方が安全（このチャットではこれで実際に未実装を発見できた）。

---

## 5. b-PACラベルのキャプション重複バグ修正＋表示切替機能の実装→撤去

### 5.1 バグ修正（維持）

- `lib/bpac-print.ts`の`buildTextFields()`：`nutritionLabel`が`content.nutritionPerUnit.label`（例：「1個あたり」）をそのまま差し込んでいたが、テンプレート側のキャプションに既に「あたり」が含まれており「1個あたりあたり」の二重表示になっていた。末尾の「あたり」を`.replace(/あたり$/, '')`で除去し、「1個」のみ差し込むように修正。
- `lib/label.ts`の`buildManufacturerBlockText()`（b-PAC専用の文字列生成関数。ブラウザ印刷用の同名関数とは別物なので注意）：「製造者：」というプレフィックスをコード側で付けていたが、これもテンプレート側キャプションと二重表示になっていたため削除。

### 5.2 表示切替機能（実装したが撤去済み）

当初、「あたり」「製造者：」「商品名：」を`captionUnit`・`captionManufacturer`・`captionProductName`という別オブジェクトとしてテンプレートに用意してもらい、チェックボックスで表示・非表示を切り替える機能（`OPTIONAL_CAPTION_TEXTS`、`printFoodLabel()`の第3引数`showCaptions`、`labels/page.tsx`の`bpacShowCaptions`state）を実装した。

しかしユーザーからの後日談で、想定していたのは「あたり・製造者・商品名の3つだけでなく、他の全項目のキャプション」であり、かつ「テンプレートにキャプション専用オブジェクトを追加する運用は結局手間で、そのままダイレクトに表示した方が分かりやすい」との結論になったため、**この切り替え機能は全て撤去した**（`lib/bpac-print.ts`・`app/dashboard/labels/page.tsx`とも元の状態＝5.1のバグ修正のみが残る状態に戻した）。

今後もし項目名の表示切替が話題に上がった場合は、「コード側で動的に出し分けるのではなく、テンプレート側で必要な項目だけキャプション込みの静的テキストとして直接作る」のが本人の結論であることを踏まえること。「商品名：」はユーザーの現テンプレートでは使っておらず、代わりに品名（`productName`オブジェクト）を直接使っている。

---

## 6. Excelインポートの80件上限を1ボタンで自動処理できるように改善

**経緯**：1リクエストあたり最大80件（Vercelのタイムアウト対策）という制限があり、ユーザーはこれまでExcelマクロで手動でファイルを分割してから1つずつインポートしていた。「アプリ側で自動的に分割処理してほしい、理想はワンボタン」という要望に対応。

**実装内容**：

- `app/api/import-export/route.ts`のPOSTに`offset`パラメータを追加。1回のリクエストでは`offset`から最大80件だけ処理し、`done`（全件処理完了 or プラン上限到達で打ち切り）と`nextOffset`をレスポンスで返す。
- 全削除（`clearAll`）・月間インポート回数カウント（`logDataTransfer`）は、一連の操作の最初のリクエスト（`offset === 0`）でのみ実行するようガード（2回目以降のチャンクで誤って多重実行されないように）。
- `parseExcelFile()`が返すファイルレベルのエラー・警告（行の値が読めない等）は、再パースするたびに毎回同じ内容が返ってくるため、最初のチャンクのレスポンスにのみ含めるようにして重複を防止。
- `app/dashboard/import/page.tsx`側は、`done`になるまで自動的に`offset`を進めながら繰り返しリクエストするループを実装。ユーザー操作はファイル選択＋ボタン1回のみで完結する。進捗表示（「取り込み中... (240/700件)」）とプログレスバーを追加。処理中にタブを閉じないよう`beforeunload`で警告。
- **副次的に発見・修正したバグ**：旧実装では80件を超えるファイルを分割せず1回で送ると、81件目以降が**警告も無いまま無言でインポートされない**状態だった（`total`には全件数が表示されるのに、実際に処理されるのは80件のみ）。現在はプラン上限（レシピ件数上限）で打ち切られた場合のみ、明示的な警告メッセージを返すように修正。
- 処理速度自体は変わらない（1件あたり約3秒の実績のまま）ため、件数が多い場合（例：700件で約35分）は依然として時間がかかる。ユーザーには「タブを閉じずに待ってほしい」旨を案内済み。処理が途中で止まっても、同じファイルを再度インポートし直せば（上書きOFFなら重複分は自動スキップされるため）続きから進められる、という設計。

---

## 7. その他

- CSV対応（インポート・エクスポートとも）は追加しない方針で確定。今後要望があれば別途対応。
- 次のチャットで`device_bash`が使えるなら、ファイル編集はそちらの方が圧倒的に効率的（このチャット・前チャットともステージ→編集→送付→コミットの手順のみで対応した）。
